{
  "name": "travel-shop-algeria",
  "version": "1.0.0",
  "private": true,
  "description": "Online Travel Agency Platform — Travel Shop Algeria",
  "workspaces": [
    "backend",
    "web",
    "mobile",
    "shared"
  ],
  "scripts": {
    "backend:dev": "npm run start:dev --workspace=backend",
    "web:dev": "npm run dev --workspace=web",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
